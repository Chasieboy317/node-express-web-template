const jwt = require('jsonwebtoken');

exports.login = async (req, res) => {
  //get salt, and password hash from db and compare to hashed password and salt
  //return login error or jwt token
}

validateInput
